# AI 伴侣 Worker 依赖
# 安装: pip install -r requirements.txt
#
# 分层安装策略：
#   必须安装:     msgpack, numpy, requests（IPC + 基础通信）
#   ASR 推荐:     faster-whisper（高精度语音识别）
#   TTS 推荐:     pyttsx3（免 GPU 离线 TTS）
#   TTS 可选:     CosyVoice（高质量，需 GPU ≥ 4GB VRAM）
#   RAG 可选:     sentence-transformers（向量嵌入，需 GPU ≥ 2GB VRAM）
#   VAD 可选:     silero-vad（语音活动检测）
#   情绪 可选:     transformers + torch + torchaudio（语音情绪识别）

# ═══════════════════════════════════════════════════════
# 必须：IPC 协议 + 基础通信
# ═══════════════════════════════════════════════════════
msgpack>=1.0.0
numpy>=1.24.0
requests>=2.31.0

# ═══════════════════════════════════════════════════════
# ASR：语音识别（至少选一个）
# ═══════════════════════════════════════════════════════
# 推荐：faster-whisper（高精度，支持 GPU/CPU，多语言）
#   pip install faster-whisper
#   模型自动下载到 ~/.cache/huggingface/
#   或手动: python download_models.py whisper
faster-whisper>=1.0.0

# ═══════════════════════════════════════════════════════
# TTS：语音合成（至少选一个）
# ═══════════════════════════════════════════════════════
# 推荐：pyttsx3（免 GPU，跨平台，离线运行）
pyttsx3>=2.90

# 可选（高质量）：CosyVoice 2（需 GPU ≥ 4GB VRAM）
#   安装后仍需下载模型: python download_models.py cosyvoice
# cosyvoice>=1.0.0

# ═══════════════════════════════════════════════════════
# RAG：向量嵌入（知识库/记忆搜索需要）
# ═══════════════════════════════════════════════════════
# 可选：sentence-transformers（需 GPU ≥ 2GB VRAM）
#   安装后仍需下载模型: python download_models.py bge-m3
# sentence-transformers>=3.0.0

# ═══════════════════════════════════════════════════════
# VAD：语音活动检测（可选，提升 ASR 体验）
# ═══════════════════════════════════════════════════════
# silero-vad>=4.0.0

# ═══════════════════════════════════════════════════════
# 情绪：语音情绪检测（可选）
# ═══════════════════════════════════════════════════════
# transformers>=4.40.0
# torch>=2.0.0
# torchaudio>=2.0.0

# ═══════════════════════════════════════════════════════
# 工具脚本（模型下载）
# ═══════════════════════════════════════════════════════
tqdm>=4.60.0
huggingface_hub>=0.20.0
